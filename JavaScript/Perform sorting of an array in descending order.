const array = [5, 2, 9, 1, 5, 6];

array.sort((a, b) => b - a);

console.log(array);

#sort this using arrow function where a and b is an array element which compared each other.
#output
[ 9, 6, 5, 5, 2, 1 ]
